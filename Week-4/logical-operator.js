// 5 > 5;

// 5 >= 5;


// let num = 3;

// num++;
// num < 5;
// num += 2;
// num <= 5

//Logical Operators
// Boolean, Comparison

// AND, OR, NOT (3 Logical Operators)

// !true;
// !false;

// let num = 3;
// num < 5;
// !(num < 5);

//AND - &&

// let num = 3;

// (num > 3) && (num < 5);
// (num < 5) && (num > 3);
// (num < 5) && (num == 3);

// OR - ||

// let num = 3;

// (num > 3) || (num < 5);
// num < 3 || num > 3;
// num < 5 || num == 3;

//AND, OR, NAND, NOR, XOR

//NAND NOT AND - !&&
//NOR NOT OR - !||

//let num = 3;

// !(num > 3) || (num < 5)
//!(num < 3 || num > 3);
// !(num < 5 || num == 3);


//XOR - Exclusive OR - 2 expression cannot be true

//Pseudocode

//num > 3 XOR num < 5 // false XOR true -> true
//num < 3 XOR num > 3 // fasle XOR false -> false
// num < 5 XOR num == 3 // true XOR true -> false

// let x = 3; 
// let y = 5;

// x < y && y > 5 && x == 3;

// x < y && !(y > 5 && x == 3);

// x < y && y > 5 || x == 3;